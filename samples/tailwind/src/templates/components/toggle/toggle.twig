<div class="relative inline-block">
  <input class="peer absolute inset-0 opacity-0" type="checkbox" name="{{id}}" id="{{id}}">
  <label class="peer-checked:after:left-6.5
  relative inline-block aspect-2/1 w-12 h-6 rounded-full border-2 bg-transparent cursor-pointer
  after:content-[''] after:block after:absolute after:size-4 after:top-0.5 after:left-1 after:rounded-full
  after:bg-current" for="{{id}}">
    <span class="text-[0px]">{{title}}</span>
  </label>
</div>
