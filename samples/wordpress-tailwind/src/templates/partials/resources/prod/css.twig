{# ws-vite names the built JS chunk after the basename of the CSS file linked
   in the layout (globals.css → globals.min.js): both bundles below share the
   "globals" basename for that reason, not by coincidence. #}
<link rel="stylesheet" href="{{site.theme.link}}/static/css/globals.min.css">
