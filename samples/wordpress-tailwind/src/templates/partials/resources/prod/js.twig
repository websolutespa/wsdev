{# See css.twig: the "globals" basename here must match the CSS entry's
   basename linked in layout.twig, or ws-vite emits a different chunk name. #}
<script type="module" src="{{site.theme.link}}/static/js/globals.min.js"></script>
