{# Placeholder — replaced by the navigation-menu + mobile sheet header in Phase 5. #}
<header class="border-b border-border">
  <div class="mx-auto flex max-w-6xl items-center justify-between px-4 py-4">
    <a href="%BASE_URL%" class="text-sm font-medium">{{ layout.site.name }}</a>
    <button type="button" data-toggle="color-scheme" aria-label="{{ labels['theme.toggle'] }}" class="rounded-md border border-border px-3 py-1.5 text-sm">
      <span class="dark:hidden">☀︎</span>
      <span class="hidden dark:inline">☾</span>
    </button>
  </div>
</header>
